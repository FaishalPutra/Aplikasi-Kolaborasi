{
  "permissions": {
    "allow": [
      "Bash(rm -f /tmp/cleanup.ts)",
      "Bash(tasklist)",
      "Bash(curl -s http://localhost:3000/api/people-to-project/health)",
      "PowerShell(Push-Location backend; npx tsc --noEmit; Write-Output \"TSC-EXIT: $LASTEXITCODE\"; Pop-Location)"
    ],
    "additionalDirectories": [
      "\\tmp"
    ]
  }
}
