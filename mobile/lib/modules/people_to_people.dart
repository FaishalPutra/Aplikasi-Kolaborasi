import 'package:flutter/material.dart';

// Modul People-to-People (Ahmad).
class PeopleToPeoplePage extends StatelessWidget {
  const PeopleToPeoplePage({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('People-to-People')));
}
