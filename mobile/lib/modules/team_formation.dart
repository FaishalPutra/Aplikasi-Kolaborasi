import 'package:flutter/material.dart';

// Modul Team Formation (Faishal).
class TeamFormationPage extends StatelessWidget {
  const TeamFormationPage({super.key});
  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Team Formation')));
}
